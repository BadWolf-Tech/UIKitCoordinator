name: Swift

on:
  push:
    branches:
    - main

# jobs:
#   build:

#     runs-on: macOS-latest

#     steps:
#     - uses: actions/checkout@v2
#     - name: Set up Xcode
#       uses: maxim-lobanov/setup-xcode@v1
#       with:
#         xcode-version: '12.x'
#     - name: Build and Test
#       run: swift test
